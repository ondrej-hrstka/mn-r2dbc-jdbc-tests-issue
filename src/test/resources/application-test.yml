datasources:
  default:
    driverClassName: org.postgresql.Driver
    schema-generate: CREATE_DROP
    dialect: POSTGRES
r2dbc:
  datasources:
    default:
      schema-generate: CREATE_DROP
      dialect: POSTGRES
---
datasources:
  default:
    db-type: postgresql

flyway:
  datasources:
    default:
      enabled: true
      db-type: postgresql
